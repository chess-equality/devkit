// Package verifyall registers the "verify-all" command. It reuses the existing
// binary to run verify for both the codex and dev-all overlays, keeping the
// cross-overlay logic centralized as the CLI is modularized.
package verifyall
